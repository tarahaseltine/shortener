<%= form_with(model: shortener, local: true) do |form| %>
  <% if shortener.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shortener.errors.count, "error") %> prohibited this shortener from being saved:</h2>

      <ul>
      <% shortener.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

Origianl "Long" URL: <%= form.text_field :original_url, id: :shortener_original_url %> <br><br>


  <div class="actions">
    <%= form.submit %>
  </div> <br>
<% end %>
